using System;


namespace Deliverable1
{

    //A program to read two integers from the user and check if corresponding place 
    //in the two numbers sums to the same total
    class Program
    {
        static void Main(string[] args)
        {
            string firstNumber = "";
            string secondNumber = "";

            Console.Write("Enter any number:");
            firstNumber = Console.ReadLine();
            Console.WriteLine("The first number is " +firstNumber);
            Console.WriteLine("Length of first number is " + firstNumber.Length);

            Console.Write("Enter another number with the same number of digits:");
            secondNumber = Console.ReadLine();
            Console.WriteLine("The second number is " + secondNumber);
            Console.WriteLine("Length of second number is " + secondNumber.Length);

            
            {

            }
        }

    }
   
}
